[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = ["cd backend && npx tsc -p backend/tsconfig.json"]

[cache]
disabled = true

[start]
cmd = "cd backend && node dist/server.js"
